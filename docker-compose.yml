# docker-compose.yml
version: '3.8'

services:
  nextjs-dev:
    build: .
    ports:
      - "3000:3000"  # Host:Container
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev